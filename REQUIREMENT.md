# 需求：Hybrid 本地电子书阅读器（Android MVP）

> 目标用户以「待机桌面只放书」的离线阅读器为准，本机阅本地 txt，无网络需求。

做一个**零权限、全程离线**的 Android 本地电子书阅读器（Hybrid MVP）：原生层是薄壳，用 WebView 承载全部 UI（HTML/CSS/JS），通过 JSBridge 让 Web 层调用原生能力（书架、导入、进度、编码探测）。

## 硬性约束（不满足即为不合格）

1. **零权限**：`AndroidManifest.xml` 不得含任何 `<uses-permission>`。
2. **APK 体积 < 1MB**。
3. **全程离线**：不得依赖任何真实 HTTP 服务器；页面全部经虚拟域名 `https://appassets.local/...` 由 `shouldInterceptRequest` 拦截路由。
4. **原生层只做薄壳**：书架/分页/翻页/字号/夜间/进度这些 UI 交互全在 Web 层实现；原生只负责 ①路由 assets 与 books ②暴露 JSBridge ③SAF 导入+编码探测落盘。
5. **文件只落 App 专属目录**：导入的书落到 `getExternalFilesDir(null)/books/`，不写公共目录。

## 功能需求（行为级，逐条可判定）

### F1 书架
- 应用启动后进入书架页（`index.html`）。
- 书架列出 App 专属目录下全部 `.txt` 书，每本显示：标题（去扩展名）、文件大小（人类可读）、阅读进度百分比。
- 目录为空时显示空状态提示文案（不是空白屏）。
- 支持删除书籍，删除需确认，确认后该书从书架与磁盘移除。

### F2 首启播种
- 首次启动（无书籍目录或目录为空）自动播种 3 本公版书：`daodejing.txt`、`lunyu.txt`、`zhuangzi.txt`，落在 `getExternalFilesDir(null)/books/`。
- 播种后书架立即能列出这 3 本，内容为 UTF-8、可正常打开阅读。

### F3 阅读与分页
- 点击书架任一书进入阅读页（`reader.html?id=<书名>`）。
- 正文以 **CSS 多列**排版（`column-width`/`column-gap` + 固定高度），整本书横向铺成多页，每屏一列一页。
- 通过 `scrollLeft` 移动实现翻页；当正文总高度超过一屏时，页数必须 ≥ 2（横向溢出 `scrollWidth > clientWidth`）。
- 底部显示页码指示 `当前 / 总页数`（如 `3 / 17`）。
- 支持**点击**翻页（点击右半屏下一页、左半屏上一页）与**左右滑动**翻页（手指左滑下一页、右滑上一页），两种都不双触发。

### F4 字号与夜间
- 字号提供 3 档，切换后当前页排版立即重排（列宽/页数按新字号重算），阅读位置尽量保持。
- 夜间模式开关切换正文/背景配色，切换后立即生效且不丢阅读进度。

### F5 进度持久化
- 阅读进度按**百分比**持久化（非页码）。
- 阅读中翻页会节流保存；离开阅读页/切后台时保存。
- 重新打开同一本书恢复到上次百分比对应的页，进度在书架卡片上也显示 `已读 N%`。

### F6 导入（SAF）
- 书架提供「导入」入口，点击后调起系统文件选择器（`Intent.ACTION_OPEN_DOCUMENT`），过滤 txt。
- 选定 txt 后原生层做**编码探测**：先严格按 UTF-8 解码，失败则回退 GB18030（GBK 超集）；自动去除 UTF-8 BOM；统一转 UTF-8 落盘。
- 导入成功后在书架立即出现；导入失败要有提示，不白屏、不崩溃。
- 同名文件再次导入：不产生重复条目（去重策略由实现决定，需可判定）。

### F7 稳定性
- 打开/翻页/切字号/夜间/导入全流程在真机无崩溃（logcat 无本应用 FATAL）。
- 弱网络或网络禁用时功能完全不受影响（本应用本就离线）。

## 非功能（默认判据）
- UI 语言为简体中文。
- 构建：可用 GitHub Actions 云端构建出可安装的 `app-debug.apk`（需 JDK 17 + SDK platform-34/build-tools;34.0.0）。

## 明确不在范围（out of scope）
- 云端书库 / 在线书城 / 任何联网能力。
- PDF、EPUB 等其它格式；仅支持纯文本 `.txt`。
- 翻页动画/特效、高亮、书签、搜索、目录。
- iOS / 桌面 / 其它平台版本。
- 多用户 / 账号 / 云同步。
- 性能基准/首启耗时优化指标。
- 阅读统计、字体自定义（仅固定 3 档）。
